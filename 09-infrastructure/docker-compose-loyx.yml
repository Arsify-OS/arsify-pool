version: '3.8'

services:
  hermes-loyx:
    image: nousresearch/hermes-agent:latest
    container_name: hermes-loyx
    command: ["/bin/bash", "/workspace/loyx-bridge.sh"]
    ports:
      - "8643:8642"
    volumes:
      - /root/regrow-up-world-dev:/workspace
      - /root/regrow-up-world-dev/loyx-config:/root/.hermes
      - /root/regrow-up-world-dev/loyx-bridge.sh:/workspace/loyx-bridge.sh
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - ORCHESTRATOR_URL=http://host.docker.internal:8000
      - ORCHESTRATOR_API_KEY=${ORCHESTRATOR_API_KEY}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
