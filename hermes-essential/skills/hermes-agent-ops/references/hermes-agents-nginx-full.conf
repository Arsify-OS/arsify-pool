# Full Nginx proxy config for Hermes Agents (hermes-agents)
# Maps internal agent ports (912x) to external proxy ports (812x)
# Rewrite this file entirely with heredoc, never append

# Proxy for Dashboard/Host (9119) - optional, adjust as needed
# server {
#     listen 8119;
#     server_name localhost;
#     location / {
#         proxy_pass http://127.0.0.1:9119;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header Origin $http_origin;
#         proxy_read_timeout 86400s;
#         proxy_buffering off;
#     }
# }

# Proxy untuk Commander (9120) via port 8120
server {
    listen 8120;
    server_name localhost;

    location / {
        proxy_pass http://127.0.0.1:9120;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 86400s;
        proxy_buffering off;
    }
}

# Proxy untuk Infrastructure (9121) via port 8121
server {
    listen 8121;
    server_name localhost;

    location / {
        proxy_pass http://127.0.0.1:9121;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 86400s;
        proxy_buffering off;
    }
}

# Proxy untuk Builder (9122) via port 8122
server {
    listen 8122;
    server_name localhost;

    location / {
        proxy_pass http://127.0.0.1:9122;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 86400s;
        proxy_buffering off;
    }
}

# Proxy untuk Plaza (9123) via port 8123
server {
    listen 8123;
    server_name localhost;

    location / {
        proxy_pass http://127.0.0.1:9123;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 86400s;
        proxy_buffering off;
    }
}

# Proxy untuk Sandbox (9129) via port 8129
server {
    listen 8129;
    server_name localhost;

    location / {
        proxy_pass http://127.0.0.1:9129;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 86400s;
        proxy_buffering off;
    }
}

# Proxy untuk Pool (9130) via port 8130
server {
    listen 8130;
    server_name localhost;

    location / {
        proxy_pass http://127.0.0.1:9130;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header Origin $http_origin;
        proxy_read_timeout 86400s;
        proxy_buffering off;
    }
}